const zerochassServerCookie = 'zerochassServerCookie';
const zerochassClientCookie = 'zerochassClientCookie';
const tokenExpiration = '30d';
const cookieExpiration = 30 * 86400 * 1000; // 30 days

export const AuthConfig = {
    zerochassServerCookie,
    zerochassClientCookie,
    tokenExpiration,
    cookieExpiration
};